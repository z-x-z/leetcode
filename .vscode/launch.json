{
  "version": "0.2.0",
  "configurations": [
    {
      // "name": "(lldb) Launch",
      // "type": "cppdbg",
      "name": "Launch1",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/out/task.out",
      "args": [],
      "cwd": "${fileDirname}"
    }
  ]
}